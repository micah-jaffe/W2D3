require 'rspec'
require 'hand'

RSpec.describe do
  describe Hand do
    subject(:hand) { Hand.new([1, 2, 3. 4, 5]) }
    describe '#initialize' do
      it "receives an array as an argument" do
        
      end
      
      it "receives card instances in its array" do
        
      end
    end
  end
end